# Portfolio V2

Redesign of the existing Zaenal Ishak portfolio.

## Replace in the existing repository
- `index.html`
- add `css/style.css`
- add `js/main.js`

The existing `img/` folder is intentionally reused. Keep the current image paths and `award.ico`.

Optional:
- Put your CV at `cv.pdf` in the repository root.
- Replace the lab placeholders with your own screenshots later.

## Features
- Light / dark mode with localStorage
- Responsive desktop / tablet / mobile layout
- Mobile navigation drawer
- Section-based navigation with hash URLs
- Project detail modal
- Professional project-first presentation
- No external framework required
